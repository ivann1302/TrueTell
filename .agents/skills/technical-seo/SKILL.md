---
name: technical-seo
description: Validate technical SEO and useful search intent for every TrueTell public page. Use when creating or changing a page, layout, metadata, service, integration, case study, article, URL, sitemap, robots rules, or structured data.
---

# Technical SEO

Optimize public pages for a user’s intent first; never generate content merely to capture keywords.

## Check each public page

- Provide a unique title and meta description, canonical URL, appropriate robots directive, Open Graph metadata, and exactly one primary H1.
- Maintain logical H2/H3 hierarchy, semantic HTML, descriptive alt text, crawlable internal links with descriptive anchors, clean URLs, and correct indexability.
- Keep sitemap and `robots.txt` aligned with public, indexable routes. Add breadcrumbs where the information architecture needs them.
- Add structured data only when it accurately applies. Use Organization, WebSite, BreadcrumbList, or other schema types only in their valid contexts.
- Prevent duplicate metadata, broken or non-crawlable internal links, thin pages, doorway-like pages, keyword stuffing, and near-duplicate integration pages.

## Integration and solution pages

Give each Bitrix24, amoCRM, МойСклад, retailCRM, 1С, AI, or other integration page independent value: specific scenarios, boundaries, affected systems, and useful implementation context. Do not mass-produce pages by swapping a product name.

## Completion

Review the rendered output and route-level metadata where available. Pair with `astro-seo-architecture` for new pages or architecture changes.
